---
import Header from "../components/Header.astro";
import "../styles/global.css";
import "@fontsource-variable/onest"

const { title, description } = Astro.props;
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<meta name="description" content={description} />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="overflow-x-hidden min-h-screen bg-[#000000] bg-[radial-gradient(#ffffff33_1px,#00091d_1px)] bg-[size:20px_20px]">
		<div class="max-w-[1200px] flex flex-col justify-center mx-auto transition-colors duration-300">
			<Header />
			<main class="flex-1 pt-20">
				<slot />
			</main>
		</div>
	</body>
</html>

<style is:global>
	:root {
		color-scheme: light dark;
	}

	html, body {
		overflow-x: hidden;
		scroll-behavior: smooth;
		font-family: "Onest Variable", system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
	}

@keyframes text-rotator {
  0%, 20%   { opacity: 0; transform: translateY(20px); }
  25%, 45%  { opacity: 1; transform: translateY(0); }
  50%, 100% { opacity: 0; transform: translateY(-20px); }
}

.rotating-text {
  position: relative;
  display: inline-block;
  vertical-align: top;
  width: 100%;
  height: auto;
}

.rotating-text__item {
  position: absolute;
  left: 0;
  top: 0;
  opacity: 0;
  animation: text-rotator 8s infinite;
  white-space: normal;
  width: 100%;
}


.rotating-text__item:nth-child(1) { animation-delay: 0s; }
.rotating-text__item:nth-child(2) { animation-delay: 2s; }
.rotating-text__item:nth-child(3) { animation-delay: 4s; }
.rotating-text__item:nth-child(4) { animation-delay: 6s; }

/* Opcional: para pantallas grandes, si quieres volver al estilo anterior */
@media (min-width: 768px) {
  .rotating-text {
    width: auto;
    height: 1.2em;
    min-width: 300px;
  }
  .rotating-text__item {
    white-space: nowrap;
    width: auto;
  }
}


	  .skills-track {
    /* Ancho = doble de lo que ocupa tu lista única */
    display: inline-flex;
    /* Animación continua sin tirones */
    animation: scroll-left 20s linear infinite;
  }

  @keyframes scroll-left {
    from { transform: translateX(0); }
    to   { transform: translateX(-50%); }
  }

	/* Boton Linkedln */
	.linkedin-btn {
		position: relative;
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 0.5rem 1rem;
		font-size: 1rem;
		font-weight: 600;
		color: #fff;
		border: 2px solid rgb(36, 41, 46);
		border-radius: 9999px;
		text-decoration: none;
		overflow: hidden;
		transition: color 0.3s ease, background-color 0.3s ease;
	}

	.linkedin-btn svg {
		flex-shrink: 0;
	}

	.linkedin-btn::before {
		content: "";
		position: absolute;
		inset: 0;
		border-radius: inherit;
		background-color: #0077b5;
		border: 2px solid #0077b5;
		transform: scaleX(0);
		transform-origin: left;
		transition: transform 0.3s ease;
		z-index: -1;
	}

	.linkedin-btn:hover::before {
		transform: scaleX(1);
	}

	/* Boton GitHub */
	.github-btn {
		position: relative;
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 0.5rem 1rem;
		font-size: 1rem;
		font-weight: 600;
		color: #fff;
		border: 2px solid rgb(36, 41, 46);
		border-radius: 9999px;
		text-decoration: none;
		overflow: hidden;
		transition: color 0.3s ease, background-color 0.3s ease;
	}

	.github-btn svg {
		flex-shrink: 0;
	}

	.github-btn::before {
		content: "";
		position: absolute;
		inset: 0;
		border-radius: inherit;
		background-color: rgb(36, 41, 46);
		transform: scaleX(0);
		transform-origin: left;
		transition: transform 0.3s ease;
		z-index: -1;
	}

	.github-btn:hover::before {
		transform: scaleX(1);
	}

	/* Boton Instagram */
	.instagram-btn {
		position: relative;
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 0.5rem 1rem;
		font-size: 1rem;
		font-weight: 600;
		color: #fff;
		border: 2px solid rgb(36, 41, 46);
		border-radius: 9999px;
		text-decoration: none;
		overflow: hidden;
		transition: color 0.3s ease, background-color 0.3s ease;
	}

	.instagram-btn svg {
		flex-shrink: 0;
	}

	.instagram-btn::before {
		content: "";
		position: absolute;
		inset: 0;
		border-radius: inherit;
		background: linear-gradient( 45deg, #405de6, #5b51db, #b33ab4, #c135b4, #e1306c, #fd1f1f);
		transform: scaleX(0);
		transform-origin: left;
		transition: transform 0.3s ease;
		z-index: -1;
	}

	.instagram-btn:hover::before {
		transform: scaleX(1);
	}

</style>